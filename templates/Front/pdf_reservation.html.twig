<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<title>Réservation -
			{{ reservation.id }}</title>
		<style>
			body {
				font-family: Arial, sans-serif;
				font-size: 14px;
			}
			h1 {
				color: #2c3e50;
				text-align: center;
			}
			.container {
				width: 80%;
				margin: auto;
				padding: 20px;
			}
			.details {
				margin-top: 20px;
			}
			table {
				width: 100%;
				border-collapse: collapse;
				margin-top: 20px;
			}
			th,
			td {
				border: 1px solid #ddd;
				padding: 10px;
				text-align: left;
			}
			th {
				background-color: #81c408;
				color: white;
			}
			.workshop-image {
				width: 100%;
				max-height: 250px;
				object-fit: cover;
				margin-top: 10px;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<h1>Réservation N°{{ reservation.id }}</h1>
			<p>Date de réservation :
				{{ reservation.reservationDate|date('d/m/Y H:i') }}</p>
			<p>Statut :
				{{ reservation.status }}</p>

			<div class="details">
				<h2>Détails du Workshop</h2>
				<table>
					<tr>
						<th>Titre</th>
						<td>{{ reservation.workshop.titre }}</td>
					</tr>
					<tr>
						<th>Description</th>
						<td>{{ reservation.workshop.description }}</td>
					</tr>
					<tr>
						<th>Prix</th>
						<td>{{ reservation.workshop.prix }}
							DT</td>
					</tr>
					<tr>
						<th>Date</th>
						<td>{{ reservation.workshop.date is not null ? reservation.workshop.date|date('d/m/Y H:i') : 'Non spécifié' }}</td>
					</tr>
					<tr>
						<th>Durée</th>
						<td>{{ reservation.workshop.duration is not null ? reservation.workshop.duration|date('H:i') : 'Non spécifié' }}</td>
					</tr>
					<tr>
						<th>Type</th>
						<td>{{ reservation.workshop.type }}</td>
					</tr>

					{# {% if reservation.workshop.type == 'Atelier Live' and reservation.workshop.user %} #}
				{# <tr>
																					<th>Formateur</th>
																					<td>{{ reservation.workshop.user.FirstName }}
																						{{ reservation.workshop.user.LastName }}</td>
																				</tr> #}
					{# {% endif %} #}
					{# 
											{% reservation.workshop.user %}
												<tr><th>
												Formateur
												</th>
													<td>
													{{ reservationS.workshop.user.FirstName ?? 'Non spécifié' }}
												{{ reservationS.workshop.user.LastName ?? 'aa' }}
											</td>
										</tr>
									{% endif %} #}
				</table>
				{# 
									{% if reservation.workshop.image %}<h2>
									Image du Workshop
									</h2>
								<img src="{{ asset('uploads/workshops/' ~ reservation.workshop.image) }}" alt="Image du Workshop" class="workshop-image"> {% endif %} #}
			</div>
		</div>
	</body>
</html>
