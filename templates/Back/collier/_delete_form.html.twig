<form method="post" action="{{ path('app_collier_delete', {'id': collier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collier.id) }}">
    <button class="btn">Delete</button>
</form>
