<form style="padding-top: 20px" method="post" action="{{ path('app_rendez_vous_delete', {'id': rendez_vou.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendez_vou.id) }}">
	<button class="btn btn-outline-dark btn-fw">Delete</button>
</form>
