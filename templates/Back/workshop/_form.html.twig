{{ form_start(form, {
    'attr': {
        'class': 'form-group',
        'novalidate': 'novalidate',
        'enctype': 'multipart/form-data'
    }
}) }}
<div class="mb-3" style="gap: 20px; width:600px">
	{{ form_label(form.titre, "Titre", {'label_attr': {'class': 'form-label'}}) }}
	{{ form_widget(form.titre, {'attr': {'class': 'form-control' ~ (form.titre.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Entrez le titre'}}) }}
	{% if form.titre.vars.errors|length > 0 %}
		<div class="text-danger mt-1">
			{% for error in form.titre.vars.errors %}
				<small>{{ error.message }}</small>
			{% endfor %}
		</div>
	{% endif %}
</div>

<div class="mb-3" style="gap: 20px; width:600px">
	{{ form_label(form.description, "Description", {'label_attr': {'class': 'form-label'}}) }}
	{{ form_widget(form.description, {'attr': {'class': 'form-control' ~ (form.description.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Entrez le titre'}}) }}
	{% if form.description.vars.errors|length > 0 %}
		<div class="text-danger mt-1">
			{% for error in form.description.vars.errors %}
				<small>{{ error.message }}</small>
			{% endfor %}
		</div>
	{% endif %}
</div>

<div class="mb-3" style="gap: 20px; width:600px">
	{{ form_label(form.date, "Date", {'label_attr': {'class': 'form-label'}}) }}
	{{ form_widget(form.date, {'attr': {'class': 'form-control' ~ (form.date.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Entrez le titre'}}) }}
	{% if form.date.vars.errors|length > 0 %}
		<div class="text-danger mt-1">
			{% for error in form.date.vars.errors %}
				<small>{{ error.message }}</small>
			{% endfor %}
		</div>
	{% endif %}
</div>

<div class="mb-3" style="gap: 20px; width:600px">
	{{ form_label(form.prix, "Prix", {'label_attr': {'class': 'form-label'}}) }}
	{{ form_widget(form.prix, {'attr': {'class': 'form-control' ~ (form.prix.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Entrez le titre'}}) }}
	{% if form.prix.vars.errors|length > 0 %}
		<div class="text-danger mt-1">
			{% for error in form.prix.vars.errors %}
				<small>{{ error.message }}</small>
			{% endfor %}
		</div>
	{% endif %}
</div>

<div class="mb-3" style="gap: 20px; width:600px">
	{{ form_label(form.theme, "Thème", {'label_attr': {'class': 'form-label'}}) }}
	{{ form_widget(form.theme, {'attr': {'class': 'form-control' ~ (form.theme.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Entrez le titre'}}) }}
	{% if form.theme.vars.errors|length > 0 %}
		<div class="text-danger mt-1">
			{% for error in form.theme.vars.errors %}
				<small>{{ error.message }}</small>
			{% endfor %}
		</div>
	{% endif %}
</div>

<div class="mb-3" style="gap: 20px; width:600px">
	{{ form_label(form.duration, "Durée", {'label_attr': {'class': 'form-label'}}) }}
	{{ form_widget(form.duration, {'attr': {'class': 'form-control' ~ (form.duration.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Entrez le titre'}}) }}
	{% if form.duration.vars.errors|length > 0 %}
		<div class="text-danger mt-1">
			{% for error in form.duration.vars.errors %}
				<small>{{ error.message }}</small>
			{% endfor %}
		</div>
	{% endif %}
</div>


<!-- Type field added -->
<div class="mb-3" style="gap: 20px; width:600px">
	{{ form_label(form.type, "Type", {'label_attr': {'class': 'form-label'}}) }}
	{{ form_widget(form.type, {'attr': {'class': 'form-control' ~ (form.type.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Entrez le titre'}}) }}
	{% if form.type.vars.errors|length > 0 %}
		<div class="text-danger mt-1">
			{% for error in form.type.vars.errors %}
				<small>{{ error.message }}</small>
			{% endfor %}
		</div>
	{% endif %}
</div>

<div class="mb-3" style="gap: 20px; width:600px">
	{{ form_label(form.nbrPlacesMax, "Nombre maximum de places", {'label_attr': {'class': 'form-label'}}) }}
	{{ form_widget(form.nbrPlacesMax, {'attr': {'class': 'form-control' ~ (form.nbrPlacesMax.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Entrez le nombre maximum de places'}}) }}
	
    {% if form.nbrPlacesMax.vars.errors|length > 0 %}
		<div class="text-danger mt-1">
			{% for error in form.nbrPlacesMax.vars.errors %}
				<small>{{ error.message }}</small>
			{% endfor %}
		</div>
	{% endif %}
</div>

<div class="mb-3" style="gap: 20px; width:600px">
	{{ form_label(form.image, "Image", {'label_attr': {'class': 'form-label'}}) }}
	{{ form_widget(form.image, {'attr': {'class': 'form-control' ~ (form.image.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Entrez le titre'}}) }}
	{% if form.image.vars.errors|length > 0 %}
		<div class="text-danger mt-1">
			{% for error in form.image.vars.errors %}
				<small>{{ error.message }}</small>
			{% endfor %}
		</div>
	{% endif %}
</div></div><button class="btn btn-outline-dark btn-fw">{{ button_label|default('Enregistrer') }}</button>{{ form_end(form) }}<script>
document.addEventListener("DOMContentLoaded", function () {
const inputs = document.querySelectorAll(".form-control");

inputs.forEach(input => {
input.addEventListener("input", function () { // Remove both is-invalid and is-valid
input.classList.remove("is-invalid", "is-valid");

// Check if input is not empty
if (input.value.trim() !== "") {
input.classList.add("is-valid"); // Add green border
}
});
});
});</script>
