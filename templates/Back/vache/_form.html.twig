{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <h1 style="color:  #F5F5F5">.................................................</h1>

    <div class="form-group">
        {{ form_label(form.race) }}
        {{ form_widget(form.race, {'attr': {'class': 'form-control', 'style': 'width: 100%;'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(form.age) }}
        {{ form_widget(form.age, {'attr': {'class': 'form-control', 'style': 'width: 100%;'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(form.EtatMedical) }}
        {{ form_widget(form.EtatMedical, {'attr': {'class': 'form-control', 'style': 'width: 100%;'}}) }}
    </div>

    <button type="submit" class="btn btn-outline-dark btn-fw">Enregistrer</button>
{{ form_end(form) }}
