{% extends 'base.html.twig' %}
{% block content %}
<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon"
        href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    {% block stylesheets %}
    {{ parent() }}

    {% endblock %}
</head>

<body>
    {% block body %}

    <h1>Vache</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vache.id }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ vache.age }}</td>
            </tr>
            <tr>
                <th>Race</th>
                <td>{{ vache.race }}</td>
            </tr>
            <tr>
                <th>EtatMedical</th>
                <td>{{ vache.EtatMedical }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vache_index') }}">back to list</a>

    <a href="{{ path('app_vache_edit', {'id': vache.id}) }}">edit</a>

    {{ include('Back/vache/_delete_form.html.twig') }}






        {% block javascripts %}
        {{ parent() }}
        {% endblock %}

        {% endblock %}
</body>

</html>
{% endblock %}